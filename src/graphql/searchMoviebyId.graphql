
query SearchMovieById($searchMovieByIdId: String!) {
  searchMovieById(id: $searchMovieByIdId) {
      Title
      Year
      imdbRating
      Genre
      Plot
      Poster
      Director
      Writer
      Actors
    
  }
}